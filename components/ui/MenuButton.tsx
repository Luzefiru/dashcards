import MaterialIcons from '@expo/vector-icons/MaterialIcons';
import { Colors } from '@/constants/Colors';

export function MenuButton() {
  return <MaterialIcons name="menu" size={32} color={Colors.foreground} />;
}
